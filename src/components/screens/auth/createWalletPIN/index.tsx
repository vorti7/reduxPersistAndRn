import React from 'react';
import {StyleSheet, View, Button} from 'react-native';

export type Props = {
};


const CreateWalletPINScreen = ({navigation}: Props) => {
    return (
        <View>
        </View>
    )
};

const styles = StyleSheet.create({
});

export default CreateWalletPINScreen;