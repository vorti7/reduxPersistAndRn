import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

export type Props = {
};


const ManageWalletScreen = ({navigation}: Props) => {
    return (
        <View>
            <Text>manage</Text>
        </View>
    )
};

const styles = StyleSheet.create({
});

export default ManageWalletScreen;